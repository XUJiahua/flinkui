# Build artifacts
/bin/

# Embedded frontend build output (generated by `make frontend`).
# A placeholder index.html is kept so `go build` works on a fresh checkout.
/web/dist/*
!/web/dist/index.html

# Frontend
/frontend/node_modules/
/frontend/.next/
/frontend/out/

# Local env / secrets (keep .env.example tracked)
.env
.env.local

.DS_Store
