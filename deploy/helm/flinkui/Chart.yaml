apiVersion: v2
name: flinkui
description: Flink Job Console — lifecycle management WebUI for FlinkDeployment jobs on the Flink Kubernetes Operator.
type: application
# Chart version — bump on chart changes.
version: 0.3.0
# Default application version (overridable via image.tag). Matches the app release.
appVersion: "0.3.0"
keywords:
  - flink
  - flink-kubernetes-operator
  - console
  - lifecycle
home: https://github.com/XUJiahua/flinkui
sources:
  - https://github.com/XUJiahua/flinkui
maintainers:
  - name: XUJiahua
